# BrozeAXE-AI Environment Variables

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Server Configuration  
PORT=5001
HOST=0.0.0.0

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
JWT_SECRET_KEY=your-secret-key-here-change-this-in-production

# Database Path (Railway will use persistent volume)
DATABASE_PATH=/data/acs_artifacts.db

# File Upload Configuration
MAX_UPLOAD_SIZE=100000000
UPLOAD_FOLDER=/data/uploads

# Anthropic AI API Key (REQUIRED for AI analysis with Claude 4.5 Sonnet)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# ===== CLOUD STORAGE CONFIGURATION =====
# Storage backend: 'local', 'gdrive', or 'dropbox'
# Default: 'local' (uses /data/uploads on Railway)
STORAGE_BACKEND=local

# --- LOCAL STORAGE (default) ---
# Base path for local file storage (Railway: /data is persistent)
STORAGE_BASE_PATH=/data

# --- DROPBOX STORAGE (recommended for cloud backup) ---
# To use Dropbox:
# 1. Set STORAGE_BACKEND=dropbox
# 2. Get access token from: https://www.dropbox.com/developers/apps
# 3. Create app -> Generate access token -> Paste below
DROPBOX_ACCESS_TOKEN=your-dropbox-access-token-here

# --- GOOGLE DRIVE STORAGE (requires Google Workspace) ---
# To use Google Drive:
# 1. Set STORAGE_BACKEND=gdrive
# 2. Create Service Account in Google Cloud Console
# 3. Share a Shared Drive with the service account email
# 4. Paste Service Account JSON credentials
# Note: Requires Google Workspace (paid) for Shared Drives
GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"..."}
GOOGLE_DRIVE_CREDENTIALS_PATH=/app/credentials.json
